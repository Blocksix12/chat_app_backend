name: chat_app_backend
description: Dart Backend API for Chat Application
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '^3.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_web_socket: ^2.0.0
  web_socket_channel: ^3.0.0
  dotenv: ^4.2.0
  dart_jsonwebtoken: ^2.14.0
  bcrypt: ^1.1.3
  supabase: ^2.2.0
  uuid: ^4.3.3

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
